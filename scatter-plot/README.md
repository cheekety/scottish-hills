# Simple-Scatter-Plot-with-Tooltip

The graph is plotted using the longitude and latitude retrieved from the csv data file.

It also has a tooltip that shows information when a mouse is hovered over.
